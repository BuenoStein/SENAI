
function refrigeranteCalc(prod, qtd) {
    
    var total1 = qtd * prod;
    document.getElementById('subRefrigerante').value = total1.toFixed(2)
}

function cebolaCalc(prod, qtd){

    var total2 = qtd * prod;
    document.getElementById('subCebola').value = total2.toFixed(2)

}

function ovosCalc(prod, qtd){
    total3 = qtd * prod;
    document.getElementById('subOvos').value = total3.toFixed(2)
}
function amacianteCalc(prod, qtd){
    total4 = qtd * prod
    document.getElementById('subAmaciante').value = total4.toFixed(2)
}
function sabaoEmPoCalc(prod, qtd){     
    total5  = qtd * prod 
    document.getElementById('subSabaoEmPo').value = total5.toFixed(2)
}
function detergenteCalc(prod, qtd){   
    total6 = qtd * prod
    document.getElementById('subDetergente').value = total6.toFixed(2)
}
function shampooCalc(prod, qtd){         
   total7 = qtd * prod
   document.getElementById('subShampoo').value = total7.toFixed(2)
}
function saboneteCalc(prod, qtd){            
    total8= qtd * prod
    document.getElementById('subSabonete').value = total8.toFixed(2)
}
function papelHigienicoCalc(prod, qtd){
           
    total9 = qtd * prod
    document.getElementById('subPapelHigienico').value = total9.toFixed(2)
}
function cremeDentalCalc(prod, qtd){
            
    total10= qtd * prod
    document.getElementById('subCremeDental').value = total10.toFixed(2)
}
function acucarCalc(prod, qtd){       
    total11= qtd * prod
    document.getElementById('subPapelHigienico').value = total11.toFixed(2)
}
function arrozCalc(prod, qtd){          
    total12 = qtd * prod
    document.getElementById('subArroz').value = total12.toFixed(2)
}
function feijaoCalc(prod, qtd){
          
    total13 = qtd * prod
    document.getElementById('subFeijao').value = total13.toFixed(2)
}
function oleoCalc(prod, qtd){
           
    total14= qtd * prod
    document.getElementById('subOleo').value = total14.toFixed(2)
}
function macarraoCalc(prod, qtd){
           
    total15= qtd * prod
    document.getElementById('subMacarrao').value = total15.toFixed(2)
}
function cafeCalc(prod, qtd){
             
    total16 = qtd * prod;
    document.getElementById('subCafe').value = total16.toFixed(2)
}
function filtroCafeCalc(prod, qtd){
          
    total17= qtd * prod
    document.getElementById('subFiltroCafe').value = total17.toFixed(2)
}
function leiteCalc(prod, qtd){
         
    total18 = qtd * prod
    document.getElementById('subLeite').value = total18.toFixed(2)
}
function manteigaCalc(prod, qtd){
             
    total19 = qtd * prod
    document.getElementById('subManteiga').value = total19.toFixed(2)
}
function margarinaCalc(prod, qtd){
             
    total20 = qtd * prod
    document.getElementById('subMargarina').value = total20.toFixed(2)
}
function yogurteCalc(prod, qtd){
              
    total21 = qtd * prod 
    document.getElementById('subYorgute').value = total21.toFixed(2)
}
function biscoitoRecheadoCalc(prod, qtd){
              
    total22  = qtd * prod
    document.getElementById('subBiscoitoRecheado').value = total22.toFixed(2)
} 
function biscoitoSalgadoCalc(prod, qtd){
      
    total23 = qtd * prod
    document.getElementById('subBiscoitoSalgado').value = total23.toFixed(2)
}
function paoFormaCalc(prod, qtd){
       
    total24 = qtd * prod
    document.getElementById('subPaoForma').value = total24.toFixed(2)
}   
function batataCalc(prod, qtd){
    
    total25= qtd * prod
    document.getElementById('subBatata').value = total25.toFixed(2)
}
function alhoCalc(prod, qtd){
  
    total26 = qtd * prod;
    document.getElementById('subAlho').value = total26.toFixed(2)
}